# === 1. 보안 및 환경 변수 (최우선 제외) ===
# API 키, DB 비밀번호 등 민감 정보
.env
# AWS 접속용 키 페어
*.pem
# 암호화 키 파일
*.key

# === 2. Python 가상 환경 및 패키지 ===
# 가상 환경 폴더 (주로 사용하는 이름)
venv/
# 표준 가상 환경 폴더
.venv/
env/
bin/
lib/
include/

# === 3. Python 캐시 및 컴파일 파일 ===
# 런타임 시 생성되는 바이트코드 캐시
__pycache__/
# .pyc, .pyo, .pyd 파일
*.py[cod]
*$py.class

# === 4. 운영체제 및 도구 설정 ===
# macOS 전용 시스템 파일
.DS_Store
# Windows 전용 썸네일 파일
Thumbs.db
# VS Code 개인 설정 (팀별로 다를 수 있음)
.vscode/
# PyCharm 설정 파일
.idea/

# === 5. 빌드 및 배포 결과물 ===
build/
dist/
*.egg-info/
